meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  additionalCommands: npm run build
  artifacts:
    "*": /
run:
  scriptName: dist/index.js
  persistenceMount: /data
  containerPort: 5000
