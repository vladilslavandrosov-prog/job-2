---
meta:
  environment: nodejs
  toolVersion: "20"
build:
  nodeJSVersion: "20"
  buildDir: "."
  buildCommand: "npm install && npm run build"
run:
  persistenceMount: /data
  containerPort: 5000
  command: "npm start"
