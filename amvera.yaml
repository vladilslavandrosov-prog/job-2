---
meta:
  environment: nodejs
  toolchain:
    name: node
    version: "20"
build:
  buildDir: .
  buildCommand: npm install && npm run build
run:
  persistenceMount: /data
  containerPort: 5000
  command: npm start
